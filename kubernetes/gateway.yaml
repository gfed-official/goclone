apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: goclone-gateway
  namespace: goclone
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "goclone.sdc.cpp"
