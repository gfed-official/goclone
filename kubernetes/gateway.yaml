apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: goclone-gateway
  namespace: goclone
spec:
  selector:
    istio: ingress
  servers:
  - port:
      number: 81
      name: http
      protocol: HTTP
    hosts:
    - "goclone.sdc.cpp"
