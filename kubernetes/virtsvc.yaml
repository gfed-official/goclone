apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: goclone-virtual-svc
  namespace: goclone
spec:
  hosts:
  - goclone.sdc.cpp
  gateways:
  - goclone-gateway
  http:
  - name: "goclone-routes"
    route:
    - destination:
        port:
          number: 8080
        host: goclone-svc
